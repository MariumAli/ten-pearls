var express = require('express');
var router = express.Router();
var stocks = require('../controllers/stocks');

// Routes related to stocks
// router.get('/', stocks.getAllUser);
// router.put('/', stocks.updateUser);
module.exports = router;